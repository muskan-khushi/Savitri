fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
python-telegram-bot==21.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0
python-dotenv==1.0.1
python-multipart==0.0.12
Pillow==10.4.0
alembic==1.13.3
# torch and torchvision omitted for local dev — disease-detection endpoint
# returns 503 "model not trained" without them, which is correct behaviour.
# Install the full requirements.txt only when you have a trained checkpoint.
